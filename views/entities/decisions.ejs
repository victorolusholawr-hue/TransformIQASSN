<% const type='decisions', label='Decisions', icon='✅', color='amber', showStatusFilter=true; %>
<% const statusOptions='[proposed,approved,deferred,rejected]'.replace('[','').replace(']','').split(',').filter(Boolean); %>
<%- include('_list', { type, label, icon, color, entities, project, member, filters, showStatusFilter, statusOptions, csrfToken }) %>
