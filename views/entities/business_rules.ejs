<% const type='business_rules', label='Business Rules', icon='📏', color='violet', showStatusFilter=false; %>
<% const statusOptions='[]'.replace('[','').replace(']','').split(',').filter(Boolean); %>
<%- include('_list', { type, label, icon, color, entities, project, member, filters, showStatusFilter, statusOptions, csrfToken }) %>
