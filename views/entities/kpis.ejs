<div class="container-wide">
  <div class="page-header">
    <div>
      <h1>KPIs &amp; Metrics <span class="count-badge"><%= entities.length %></span></h1>
      <p class="page-sub"><a href="/projects/<%= project.id %>"><%= project.name %></a></p>
    </div>
    <a href="/projects/<%= project.id %>" class="btn-outline">Back to Project</a>
  </div>

  <% if (!entities.length) { %>
    <div class="card empty-state"><p>No KPIs extracted yet.</p></div>
  <% } else { %>
    <div class="section">
      <div class="table-wrap">
        <table class="students-table entity-table">
          <thead><tr><th>KPI</th><th>Description</th><th>Target</th><th>Frequency</th><th>Owner</th><th>Confidence</th><th></th></tr></thead>
          <tbody>
            <% for (const e of entities) { const conf = Number(e.confidence || 0.7); %>
              <tr>
                <td class="cell-title"><a href="/projects/<%= project.id %>/kpis/<%= e.id %>"><%= e.name %></a></td>
                <td class="cell-meta"><%= e.description ? e.description.slice(0, 80) + (e.description.length > 80 ? '...' : '') : '—' %></td>
                <td class="cell-meta"><%= e.target_value || '—' %></td>
                <td><span class="badge-info"><%= e.frequency || '—' %></span></td>
                <td class="cell-meta"><%= e.owner || '—' %></td>
                <td><span class="confidence-badge <%= conf >= 0.8 ? 'confidence-badge-high' : conf >= 0.5 ? 'confidence-badge-med' : 'confidence-badge-low' %>"><%= Math.round(conf * 100) %>%</span></td>
                <td>
                  <% if (member.role !== 'viewer') { %>
                    <form method="POST" action="/projects/<%= project.id %>/kpis/<%= e.id %>/delete" onsubmit="return confirm('Delete this KPI?')">
                      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                      <button class="btn-danger btn-sm">Del</button>
                    </form>
                  <% } %>
                </td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  <% } %>
</div>
