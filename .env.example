NODE_ENV=development
PORT=3000

# Flask session equivalent
SESSION_SECRET=change_me_to_a_random_32_char_string

# SQL Server (Docker service name = "db")
DB_SERVER=db
DB_PORT=1433
DB_NAME=transformiq_assn
DB_USER=sa
DB_PASSWORD=YourStrong!Passw0rd

# Anthropic Claude
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-opus-4-7

# Azure Blob Storage — leave blank to use local disk in dev
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER=transformiq-assn

# Okta OIDC — add values when wiring in SSO (Phase 2)
# OKTA_DOMAIN=
# OKTA_CLIENT_ID=
# OKTA_CLIENT_SECRET=
# OKTA_CALLBACK_URL=http://localhost:3000/auth/callback
